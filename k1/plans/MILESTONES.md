# K1 里程碑

> 验收明确、单步可回退。**仿真/训练不搬上板**(已在上级 README 定语)。
> 上游参考:K3 的 [`../../K3/plans/MILESTONES.md`](../../K3/plans/MILESTONES.md)(P0 已于 2026-09-08 跑通)。

## P0' · 环境与构建  完成(2026-09-09)

| 步 | 内容 | 状态 |
|---|---|---|
| 1 | 板卡确认(MUSE-Pi-Pro / X60 / Bianbu 2.3.3) |  2026-09-08 |
| 2 | apt 依赖 + rustup 1.98.1 |  2026-09-08 |
| 3 | 源码转储(本机 tar → scp → `/opt/microduck-k1`) |  2026-09-08 |
| 4 | 构建产出 `robotd` 等二进制(当时裁 `mediad`;2026-09-11 起全量) |  **43m39s / 313 crate / robotd 7 319 888 B** |
| 5 | **`ort` 吃上可用的 `libonnxruntime.so`(R1)** |  **已解决** —— K1 的 `python3-spacemit-ort` 自带 **1.24.0**,探针验证 API v23 可用 |

## P1' · 策略链  核心完成(2026-09-09)

| 步 | 内容 | 状态 |
|---|---|---|
| 1 | `robotd --sim` 接本地 duck-body(反向隧道) |  自动重连、环率 46.5–48.5 Hz |
| 2 | 官方 9 个策略 ONNX 加载 | 是 |
| 3 | 复刻:坐姿起身 → 站立 → 行走 |  起身 z 0.070→0.116;**行走 1.245 m 未摔**;转向成功 |
| 4 | 环率 / 位移 / 速度跟踪偏差记录 |  见 [`../docs/05-联调实录.md`](../docs/05-联调实录.md) |
| 5 | 与 K3 同场景算力对比 |  见下表 |
| 6 | Web 控制台(浏览器摇杆操控) |  `:8081`,实测驱动 1.44 m |

## P1'+ · 感知:检测器接上板载 NPU  完成(2026-09-10)

| 步 | 内容 | 状态 |
|---|---|---|
| 1 | EP 加载器(`duck-detect/src/spacemit.rs`,新增 256 行) |  加载成功,横幅 `spacemit ep · onnxruntime 1.24.2+spacemit.a1` |
| 2 | 模型 opset 12 → 17(`version_converter`) |  通过,且与原图**逐位相同**(12 帧 × 10 500 输出,最大绝对差 **0.000e+00**) |
| 3 | EP vs CPU 延迟 / CPU 占用 |  p50 **142.0 ms vs 718.5 ms(5.1×)**;每帧 CPU 279.7 vs 1 439.7 ms |
| 4 | 检测结果等价性(阈值 0.05) |  4/12 帧出框,**框坐标逐个相同**(frame_09 概率 0.13 vs 0.14) |
| 5 | `mediad` 的检测器选后端时优先用 EP |  已改;`--exclude duck-detect` 已从两处构建脚本去掉 |
| 6 | 待办:真机上与控制环抢核的实测 |  2 Hz 下 EP 仍占 **56% 单核**,要在真机上量 |

细节见 [`../docs/05-联调实录.md`](../docs/05-联调实录.md)。

## P2' · 真机评估(需硬件)

1. UART 引脚/电平核对(1 Mbps Dynamixel 总线)
2. 供电方案 + 尺寸试装(MUSE-Pi-Pro vs K3 Pico-ITX)
3. **实时性评估**:K1 **无 RCPU 实时核**(K3 有 600 MHz RCPU 直挂 CAN-FD/TSN),
   舵机总线的抖动需实测
4. systemd unit 集(Bianbu 2.3.3)

## K3 / K1 对照基线(2026-09-09 实测)

| 指标 | K3(实测) | K1(实测) |
|---|---|---|
| 构建耗时 | **6m42s** | **43m39s**(内存 3.8 GiB 无 swap,并行度自相残杀) |
| 编译 crate | — | 313 |
| `robotd` 产物 | 7.3 MB | 7 319 888 B |
| 环率(空载 `--fake`) | 49.0 of 50 Hz · 0 missed | **50.0 of 50 Hz · 0 missed** |
| 环率(`--sim` 隧道) | 47.9 of 50 Hz | 46.5–48.5 of 50 Hz |
| 满载最低(含隧道) | 未低于 45 | 40.7 Hz |
| **板载环率(无隧道)** | 未测 | **50.0 of 50 Hz · 0 missed · 1.9% 单核**(2026-09-10 补测) |
| 起身 z | 0.065 → 0.116 | 0.070 → 0.116 |
| 站立稳定 | 7 s | 是 |
| 行走 | 1.145 m | **1.245 m** |
| 速度跟踪 | 39%(0.097 vs 0.25) | 同源(仿真执行器简化) |
| CPU 温度 | 64 °C | **50 °C** |
| ORT 版本 | 1.24.2+spacemit.a1  | **1.24.0+spacemit.a3**(自带包) |
| **检测器 / NPU** | 未测 | **142.0 ms p50 · 56% 单核 @2 Hz**(2026-09-10 补测) |
| 检测器 / CPU 对照 | 未测 | 718.5 ms p50 —— EP **快 5.1×** |
| Web 控制台 |  `:8081` |  `:8081` |
